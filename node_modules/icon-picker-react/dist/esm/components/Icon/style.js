import styled, { css } from "styled-components";
export const Content = styled.span `
  color: ${({ color }) => color && color};
  ${({ size }) => size ? css `
    font-size: ${size}rem;
  `
    :
        css `
    font-size: 1rem;
  `};
`;
